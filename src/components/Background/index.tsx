import React from 'react';
import { BackgroundContainer } from './styles';

const Background = () => {
  return (
    <BackgroundContainer>
      {/* Tu contenido aquí */}
    </BackgroundContainer>
  );
};

export default Background;
